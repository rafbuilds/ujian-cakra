// frontend/config.js
const CONFIG = {
  // Kosong = pakai domain yang sama (backend serve frontend)
  // Tidak perlu ganti apapun saat deploy ke Render
  API_BASE: "",
  DEV_MODE: false,
  BASE_PATH: "",
};

const Config = CONFIG;
